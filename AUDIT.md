# Number Core 総点検メモ

## 2026-09-05

- モンスター追加処理は `monster-expansion.js` に集約。
- ページ初回表示で追加の `reset()` を実行しないよう変更。二重のバトル数加算を防止。
- `monster-expansion.js` の旧オーバーレイ処理は無効化コメントのみのファイルにして、実処理は残さない。
- `monster-ability-audit.js` を追加し、宣言した能力に実際の効果を与える。
- 画像参照は `navi-character-` の fantasy/monsters 配下を使用。
- 主要な実在ファイルをリポジトリ上で確認済み。
- `index.html` は既存ゲームUIを維持し、能力レイヤーのみ追加読み込み。

残課題として、ブラウザ実機での全分岐テスト（連続ターン、希少出現率、タブレット各幅）を目視確認する。
